[build]
  command = "NODE_ENV=production gatsby build"
  publish = "public/"

[template]
  incoming-hooks = ["Ghost"]

[plugins]
  package = "netlify-plugin-cloudinary"

  [plugins.inputs]
  cloudName = "dlq71ih0t"